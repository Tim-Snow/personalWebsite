const style = {
  fullWidth: {
    width: '100%',
    display: 'flex',
  },
  row: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    flex: 1,
  },
} as { [key: string]: React.CSSProperties };

export default style;
