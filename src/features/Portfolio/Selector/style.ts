const style = {
  content: {
    display: 'flex',
    flexWrap: 'nowrap',
    overflow: 'hidden',
    overflowX: 'auto',
    scrollbarWidth: 'none',
  },
} as { [key: string]: React.CSSProperties };

export default style;
