const style = {
  content: {
    display: 'flex',
    flexWrap: 'nowrap',
    overflow: 'hidden',
    overflowX: 'auto',
    scrollbarWidth: 'none',
  },
  spinner: { position: 'relative', top: 50 },
} as { [key: string]: React.CSSProperties };

export default style;
